A top level heading, used for page headers, application titles etc.

**Note:** For accessibility reasons, any given page should only ever have one(1)
`<PageTitle>` component.

```jsx
import { PageTitle } from 'mark-one';
<PageTitle>Hello World</PageTitle>
```
